server:
  port: 8003

spring:
  config:
    use-legacy-processing: true
  cloud:
    nacos:
      server-addr: localhost:8848
    config:
      file-extension: yml
  application:
    name: cartisan-mall-content
  profiles:
    include: web, mysql
    active: local

